이름공간 : namespace . 흔히 쓰는 std 라는 이름공간에는 cout cin 등이 있다. 이걸 가져오려면 std::cout 이렇게씀
이걸 번거롭게 안하려고 using 을 사용

탬플릿 : 코드 편하게. 솔직히 잘 모르겠음 예제 몇개 봐야할듯

생성자. 소멸자  : 생성자는 변수가 생성될 때 소멸자는 변수가 소멸될 때 자동으로 호출된다. 직접 만들기 가능- 다른방법몰라

초기설정 : 생성자에서 변수에 값을 설정하고자 할 때 사용. 그저 대입할때와 의미가 같은 때가 있지만,
변수가 const 로 선언되어 있다면 초기설정 방식을 사용한다
 
맴버함수 뒤에 붙는 const : 이 함수를 호출해도 클래스 멤버는 변경되지 않는다고 컴파일러에 알리기
내용이 변경되지 않는다고 보증하는것.

delete 와 delete[] : delete 냐옹이